ec2tools
========

boto is great. But boto isn't for every day use by humans. Boto is a low-level interface, practically assembly language.

Why are you using assembly language?

ec2tools is a higher-level library, with useful abstractions and a rich API. So you can use AWS like a human.
